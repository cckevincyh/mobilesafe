<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- 在去头的同时,还保留高版本的样式主题 -->
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
    </style>
    
 <!-- <TextView 
        android:text="功能列表"
        android:gravity="center"
        android:textSize="20sp"
        android:textColor="#000"
        android:padding="10dp"
        android:background="#0f0"
        android:layout_width="match_parent"
    	android:layout_height="wrap_content"/> --> 
    <style name="TitleStyle">
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#000</item>
        <item name="android:padding">10dp</item>
        <item name="android:background">#0f0</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    
    
     <!-- <Button
	   	    android:text="下一页"
	   	    android:onClick="nextPage"
	   	    android:layout_alignParentRight="true"
	   	    android:layout_alignParentBottom="true"
	   	    android:drawableRight="@drawable/next"
	   	    android:background="@drawable/selector_next_btn_bg"
	   	    android:layout_width="wrap_content"
	   	    android:layout_height="wrap_content"/> -->
	<style	name="preBtn">
	    <item name="android:text">上一页</item>
        <item name="android:onClick">prePage</item><!-- 触发事件 -->
        <item name="android:layout_alignParentLeft">true</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:drawableLeft">@drawable/previous</item>
        <item name="android:background">@drawable/selector_next_btn_bg</item><!-- 在drawable目录下编辑上一页图片按钮选择器 -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
	</style>
	<style	name="nextBtn">
	    <item name="android:text">下一页</item>
        <item name="android:onClick">nextPage</item><!-- 触发事件 -->
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:drawableRight">@drawable/next</item>
        <item name="android:background">@drawable/selector_next_btn_bg</item><!-- 在drawable目录下编辑下一页图片按钮选择器 -->
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
	</style>
</resources>
